from setuptools import setup, find_packages

setup(name='db_interface',
      version='0.0',
      description='CargoCast database interface',
      packages=find_packages(),
      author_email='pascal.dietrich@cargocast.de',
      zip_safe=False)
