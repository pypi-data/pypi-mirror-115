from setuptools import setup

setup(name='my_statistics_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['my_statistics_distributions'],
      zip_safe=False)
