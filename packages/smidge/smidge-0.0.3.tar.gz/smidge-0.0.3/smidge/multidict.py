import collections
# this concept could be generalisable
# to a dict of arbitrary iterables
# this one uses a set
#   doing arbitrary iterables would mean
#   turning the get-one-ability
#   of set.py into 
class multidict(collections.defaultdict):
