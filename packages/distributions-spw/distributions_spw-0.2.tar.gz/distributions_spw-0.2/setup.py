from setuptools import setup

setup(name='distributions_spw',
      version='0.2',
      description='Gaussian and Binomial distributions',
      packages=['distributions_spw'],
      zip_safe=False)
